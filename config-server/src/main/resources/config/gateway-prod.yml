server:
  port: 80

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
#      路由路径
      routes:
  boot:
    admin:
      client:
        url: http://localhost:8001
